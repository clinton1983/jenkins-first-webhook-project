pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself elvisco'
                sh 'echo Integrating Jenkins Pipeline with GitHub Webhook using Jenkinsfile'
                sh 'cat /etc/os-release'
            }
        }
    }
}
